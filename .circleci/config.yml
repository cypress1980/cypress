version: 2.1
orbs:
  cypress: cypress-io/cypress@1
  
workflows:
  build:
    jobs:
      - cypress/run:
          yarn: true
          record: true
          store_artifacts: true
          parallel: true
          parallelism: 2
          post-steps:
            - store_artifacts:
                path: reports
